# Busca no acervo do DOU — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Uma tela de busca por termo sobre todo o acervo do DOU, com facetas com contagem, trecho destacado e tratamento correto de CNPJ e número de processo.

**Architecture:** Um serviço (`dou_search_service`) é dono de um índice Meilisearch dedicado, no molde do `impugnacao_reference_search`. O núcleo que extrai e classifica identificadores é função pura, testável sem rede. O banco continua sendo a fonte da verdade: o índice é descartável e reconstruível.

**Tech Stack:** Meilisearch (via `meilisearch_python_sdk`, já dependência), Flask 3.1, SQLAlchemy, Jinja2 + Bootstrap 5.

**Spec:** `docs/superpowers/specs/2026-08-10-busca-dou-design.md`

## Global Constraints

- **Sem novas dependências.** `meilisearch_python_sdk` já é usada por `impugnacao_reference_search`.
- **Gerenciador é `uv`.** Nunca `pip`. Rodar tudo com `uv run python ...`.
- **Indexar nunca derruba a captura**, e **buscar nunca derruba a tela.** Falha do Meilisearch é sempre degradação graciosa, nunca exceção que sobe.
- **O índice de teste NUNCA é o de produção.** Usar `dou_articles_test`, apagado ao final. Neste módulo já houve um teste que destruiu dados reais por usar a mesma chave que o dado verdadeiro.
- **Contagens de dígitos conferidas contra o acervo real:** CNPJ = 14, processo administrativo = 17.
- **`pub_date_num` é `AAAAMMDD` inteiro** (10/08/2026 → `20260810`), não timestamp — timestamp depende de fuso e erra o dia no filtro.
- **O acervo é global, sem `law_firm_id`.** Só a lista de clientes do seletor filtra por escritório.
- **Sem framework de testes.** Scripts executáveis em `tests/`, com o helper `check(nome, condicao, detalhe)`.

---

## Estrutura de arquivos

| Arquivo | Responsabilidade |
|---|---|
| `app/services/dou_search_service.py` | **Criar.** Dono do índice: extração de identificadores, classificação da consulta, indexação, busca. |
| `scripts/reindex_dou.py` | **Criar.** CLI de reindexação em lote. |
| `app/blueprints/dou.py` | **Modificar.** Rota `GET /dou/busca`. |
| `templates/dou/busca.html` | **Criar.** A tela. |
| `static/css/dou.css` | **Modificar.** Estilos das facetas e do destaque. |
| `templates/partials/sidebar.html` | **Modificar.** Item "Busca" no submenu do módulo. |
| `app/services/dou_ingestion_service.py` | **Modificar.** Indexar após o commit de cada `(dia, seção)`. |
| `tests/test_dou_search.py` | **Criar.** Núcleo puro + integração com Meilisearch. |
| `CLAUDE.md`, `docs/MANUAL_DIARIO_OFICIAL.md` | **Modificar.** Documentação. |

---

## Task 1: Núcleo puro — identificadores e classificação da consulta

A peça que decide se a busca serve. Sem rede, sem banco, sem Flask.

**Files:**
- Create: `app/services/dou_search_service.py`
- Test: `tests/test_dou_search.py`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `so_digitos(valor: str | None) -> str`
  - `extrair_cnpjs(texto: str | None) -> list[str]`
  - `extrair_processos(texto: str | None) -> list[str]`
  - `orgao_raiz(hierarquia: str | None) -> str | None`
  - `classificar_consulta(termo: str) -> tuple[str, str]` — `('cnpj'|'processo'|'texto', termo_normalizado)`

- [ ] **Step 1: Escrever o teste que falha**

Criar `tests/test_dou_search.py`:

```python
#!/usr/bin/env python3
"""
Testes da busca do DOU (app/services/dou_search_service.py).

As partes 1 a 4 são função pura: sem rede, sem banco, sem Flask.

    uv run python tests/test_dou_search.py
"""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from app.services import dou_search_service as busca

_falhas = []


def check(nome: str, condicao: bool, detalhe: str = '') -> None:
    if condicao:
        print(f'  ✅ {nome}')
    else:
        print(f'  ❌ {nome}{" — " + detalhe if detalhe else ""}')
        _falhas.append(nome)


def test_so_digitos():
    print('\n1. Normalização para dígitos')
    check('tira pontuação do CNPJ',
          busca.so_digitos('19.630.496/0001-05') == '19630496000105',
          busca.so_digitos('19.630.496/0001-05'))
    check('tira pontuação do processo',
          busca.so_digitos('15414.630210/2026-80') == '15414630210202680',
          busca.so_digitos('15414.630210/2026-80'))
    check('None vira string vazia', busca.so_digitos(None) == '')
    check('texto sem dígito vira vazio', busca.so_digitos('Previdência') == '')


def test_extrair_cnpjs():
    print('\n2. Extração de CNPJ do texto')
    texto = ('Autoriza o funcionamento de BESSO RE BRASIL CORRETORA LTDA, '
             'CNPJ nº 19.630.496/0001-05, com sede na cidade do Rio.')
    check('acha o CNPJ formatado e normaliza',
          busca.extrair_cnpjs(texto) == ['19630496000105'],
          str(busca.extrair_cnpjs(texto)))

    dois = 'CNPJ: 04.898.857/0002-02 e também 05.917.351/0001-85.'
    check('acha os dois', busca.extrair_cnpjs(dois) == ['04898857000202', '05917351000185'],
          str(busca.extrair_cnpjs(dois)))

    check('acha CNPJ já em dígitos',
          busca.extrair_cnpjs('inscrito sob 19630496000105 nesta data') == ['19630496000105'],
          str(busca.extrair_cnpjs('inscrito sob 19630496000105 nesta data')))

    check('não inventa CNPJ onde não há',
          busca.extrair_cnpjs('Portaria nº 1.234, de 8 de agosto') == [],
          str(busca.extrair_cnpjs('Portaria nº 1.234, de 8 de agosto')))

    check('não duplica o mesmo CNPJ',
          busca.extrair_cnpjs('19.630.496/0001-05 e de novo 19.630.496/0001-05') ==
          ['19630496000105'],
          str(busca.extrair_cnpjs('19.630.496/0001-05 e de novo 19.630.496/0001-05')))

    check('texto vazio devolve lista vazia', busca.extrair_cnpjs(None) == [])


def test_extrair_processos():
    print('\n3. Extração de número de processo')
    texto = 'Processo nº 15414.630210/2026-80, referente à contestação.'
    check('acha e normaliza',
          busca.extrair_processos(texto) == ['15414630210202680'],
          str(busca.extrair_processos(texto)))
    check('17 dígitos, não 14',
          len(busca.extrair_processos(texto)[0]) == 17,
          str(len(busca.extrair_processos(texto)[0])))
    check('não confunde CNPJ com processo',
          busca.extrair_processos('CNPJ nº 19.630.496/0001-05') == [],
          str(busca.extrair_processos('CNPJ nº 19.630.496/0001-05')))


def test_classificar_consulta():
    print('\n4. Roteamento da consulta')
    for entrada in ('19.630.496/0001-05', '19630496000105', ' 19.630.496/0001-05 '):
        tipo, termo = busca.classificar_consulta(entrada)
        check(f'{entrada!r} vai para o campo de CNPJ',
              (tipo, termo) == ('cnpj', '19630496000105'), f'{tipo}/{termo}')

    tipo, termo = busca.classificar_consulta('15414.630210/2026-80')
    check('processo vai para o campo de processo',
          (tipo, termo) == ('processo', '15414630210202680'), f'{tipo}/{termo}')

    tipo, termo = busca.classificar_consulta('Fator Acidentário de Prevenção')
    check('texto livre continua texto', tipo == 'texto', tipo)

    # O guarda que evita falso positivo: número embutido numa frase é texto
    tipo, _ = busca.classificar_consulta('portaria 19630496000105 de agosto')
    check('número dentro de frase NÃO vira busca de CNPJ', tipo == 'texto', tipo)

    tipo, _ = busca.classificar_consulta('12345')
    check('número curto continua texto', tipo == 'texto', tipo)

    tipo, termo = busca.classificar_consulta('   ')
    check('termo em branco é texto vazio', (tipo, termo) == ('texto', ''), f'{tipo}/{termo}')


def test_orgao_raiz():
    print('\n5. Raiz da hierarquia do órgão')
    h = 'Ministério da Previdência Social/Instituto Nacional do Seguro Social/Diretoria'
    check('pega o primeiro nível',
          busca.orgao_raiz(h) == 'Ministério da Previdência Social', busca.orgao_raiz(h))
    check('sem barra devolve o próprio',
          busca.orgao_raiz('Presidência da República') == 'Presidência da República')
    check('None devolve None', busca.orgao_raiz(None) is None)
    check('vazio devolve None', busca.orgao_raiz('   ') is None)


def main():
    print('=' * 60)
    print('TESTES DA BUSCA DO DOU')
    print('=' * 60)

    test_so_digitos()
    test_extrair_cnpjs()
    test_extrair_processos()
    test_classificar_consulta()
    test_orgao_raiz()

    print('\n' + '=' * 60)
    if _falhas:
        print(f'❌ {len(_falhas)} falha(s): {", ".join(_falhas)}')
        return 1
    print('✅ Todos os testes passaram')
    return 0


if __name__ == '__main__':
    sys.exit(main())
```

- [ ] **Step 2: Rodar o teste e confirmar que falha**

```bash
uv run python tests/test_dou_search.py
```

Esperado: `ImportError: cannot import name 'dou_search_service'`.

- [ ] **Step 3: Implementar o núcleo puro**

Criar `app/services/dou_search_service.py`:

```python
"""
Busca no acervo do Diário Oficial (Meilisearch).

Índice dedicado, alimentado pela ingestão e reconstruível a partir do banco. O
MySQL é a fonte da verdade; este índice é descartável. Daí as duas regras que
valem em todo o arquivo: indexar nunca derruba a captura, buscar nunca derruba
a tela.

O ponto central é o tratamento de identificadores. O Meilisearch tokeniza
separando em '.', '/' e '-', então `19.630.496/0001-05` vira os tokens 19, 630,
496, 0001, 05 e quem digitasse `19630496000105` não acharia nada — em 31% do
acervo, que é a fatia que contém CNPJ. Por isso os identificadores são
extraídos, normalizados para só dígitos e guardados em campos próprios, e a
consulta é roteada para esses campos quando o termo é um número.
"""

from __future__ import annotations

import re

# Formatos conferidos contra o acervo real:
#   CNPJ     19.630.496/0001-05     -> 14 dígitos
#   processo 15414.630210/2026-80   -> 17 dígitos
_RE_CNPJ = re.compile(r'\d{2}\.\d{3}\.\d{3}/\d{4}-\d{2}')
_RE_CNPJ_DIGITOS = re.compile(r'(?<!\d)\d{14}(?!\d)')
_RE_PROCESSO = re.compile(r'\d{5}\.\d{6}/\d{4}-\d{2}')

TAM_CNPJ = 14
TAM_PROCESSO = 17

# Pontuação aceita num termo que ainda assim é "só um número"
_PONTUACAO_DE_NUMERO = re.compile(r'[\s.\-/]')


def so_digitos(valor: str | None) -> str:
    """'19.630.496/0001-05' → '19630496000105'."""
    return re.sub(r'\D', '', valor or '')


def _unicos(valores) -> list[str]:
    """Preserva a ordem de aparição e remove repetidos."""
    vistos, saida = set(), []
    for v in valores:
        if v not in vistos:
            vistos.add(v)
            saida.append(v)
    return saida


def extrair_cnpjs(texto: str | None) -> list[str]:
    """Todos os CNPJs do texto, normalizados. Cobre as duas grafias."""
    if not texto:
        return []
    achados = [so_digitos(m) for m in _RE_CNPJ.findall(texto)]
    achados += _RE_CNPJ_DIGITOS.findall(texto)
    return _unicos(achados)


def extrair_processos(texto: str | None) -> list[str]:
    """Todos os números de processo administrativo, normalizados."""
    if not texto:
        return []
    return _unicos(so_digitos(m) for m in _RE_PROCESSO.findall(texto))


def orgao_raiz(hierarquia: str | None) -> str | None:
    """Primeiro nível de 'Ministério X/Autarquia Y/Diretoria Z'.

    A hierarquia completa tem centenas de valores distintos e não vira faceta
    usável; a raiz tem dezenas.
    """
    if not hierarquia:
        return None
    return (hierarquia.split('/')[0] or '').strip() or None


def classificar_consulta(termo: str) -> tuple[str, str]:
    """Decide por qual campo a consulta vai. Devolve (tipo, termo_normalizado).

    tipo é 'cnpj', 'processo' ou 'texto'.

    O identificador só é reconhecido quando o termo **inteiro** é o número —
    aceitando pontuação e espaço. Sem esse guarda, "portaria 19630496000105 de
    agosto" viraria busca de CNPJ e perderia o resto da frase.
    """
    termo = (termo or '').strip()
    if not termo:
        return ('texto', '')

    digitos = so_digitos(termo)
    if digitos and not _PONTUACAO_DE_NUMERO.sub('', termo).strip(digitos):
        if len(digitos) == TAM_CNPJ:
            return ('cnpj', digitos)
        if len(digitos) == TAM_PROCESSO:
            return ('processo', digitos)

    return ('texto', termo)
```

- [ ] **Step 4: Rodar o teste e confirmar que passa**

```bash
uv run python tests/test_dou_search.py
```

Esperado: `✅ Todos os testes passaram`, exit 0.

Se `classificar_consulta` falhar no caso "número dentro de frase", o guarda está
errado: a condição correta é que o termo, sem a pontuação aceita, seja composto
apenas dos dígitos — qualquer letra remanescente o torna texto.

- [ ] **Step 5: Conferir a extração contra o acervo real**

Não é teste automatizado; é a checagem de que as regex casam com o dado de
verdade, e não só com a fixture.

```bash
uv run python -c "
from main import app
from app.models import DouArticle
from app.services import dou_search_service as b
with app.app_context():
    amostra = DouArticle.query.limit(3000).all()
    com_cnpj = sum(1 for a in amostra if b.extrair_cnpjs(a.texto))
    com_proc = sum(1 for a in amostra if b.extrair_processos(a.texto))
    print(f'{com_cnpj} de {len(amostra)} com CNPJ ({com_cnpj*100//len(amostra)}%)')
    print(f'{com_proc} de {len(amostra)} com processo ({com_proc*100//len(amostra)}%)')
    raizes = {b.orgao_raiz(a.orgao_hierarquia) for a in amostra}
    print(f'{len(raizes)} órgãos-raiz distintos (faceta usável se for algumas dezenas)')
"
```

Esperado: ~31% com CNPJ, ~49% com processo, e algumas dezenas de órgãos-raiz.
Se os órgãos-raiz passarem de ~100, a faceta de órgão precisa de outro corte —
pare e discuta antes de seguir.

- [ ] **Step 6: Commit**

```bash
git add app/services/dou_search_service.py tests/test_dou_search.py
git commit -m "feat(dou): núcleo da busca — extração e roteamento de identificadores

O Meilisearch tokeniza separando em '.', '/' e '-', então 19.630.496/0001-05
vira cinco tokens e quem digitasse 19630496000105 não acharia nada — em 31%
do acervo, que é a fatia com CNPJ.

Identificadores são extraídos e normalizados para só dígitos, e a consulta é
roteada para o campo certo quando o termo inteiro é um número. O guarda do
'termo inteiro' evita que 'portaria 19630496000105 de agosto' vire busca de
CNPJ e perca o resto da frase.

Contagens conferidas contra o acervo real: CNPJ 14 dígitos, processo 17."
```

---

## Task 2: Índice e indexação

**Files:**
- Modify: `app/services/dou_search_service.py`
- Modify: `app/services/dou_ingestion_service.py`
- Create: `scripts/reindex_dou.py`
- Test: `tests/test_dou_search.py` (acrescentar a parte de integração)

**Interfaces:**
- Consumes: `so_digitos`, `extrair_cnpjs`, `extrair_processos`, `orgao_raiz` da Task 1.
- Produces:
  - `is_available() -> bool`
  - `montar_documento(artigo) -> dict`
  - `index_articles(artigos, indice=None) -> int` — devolve quantos foram indexados
  - `reindex_all(desde=None, lote=1000, indice=None) -> int`
  - `MEILI_INDEX` (nome do índice, de `DOU_MEILI_INDEX` ou `dou_articles`)

- [ ] **Step 1: Escrever o teste de integração que falha**

Acrescentar a `tests/test_dou_search.py`, antes de `def main():`:

```python
def test_documento_indexado():
    """O documento carrega o que a tela precisa e o que a busca filtra."""
    print('\n6. Montagem do documento')
    from main import app
    from app.models import DouArticle

    with app.app_context():
        artigo = (DouArticle.query
                  .filter(DouArticle.orgao_hierarquia.isnot(None)).first())
        if artigo is None:
            print('  ⏭️  acervo vazio — pulando')
            return

        doc = busca.montar_documento(artigo)

    check('id é o id da matéria', doc['id'] == artigo.id, str(doc.get('id')))
    check('pub_date_num é AAAAMMDD inteiro',
          isinstance(doc['pub_date_num'], int) and 20000000 < doc['pub_date_num'] < 21000000,
          str(doc.get('pub_date_num')))
    check('data_br pronta para exibir',
          doc['data_br'] == artigo.pub_date.strftime('%d/%m/%Y'), doc.get('data_br'))
    check('orgao_raiz é o primeiro nível',
          doc['orgao_raiz'] == busca.orgao_raiz(artigo.orgao_hierarquia),
          doc.get('orgao_raiz'))
    check('cnpjs é lista', isinstance(doc['cnpjs'], list))
    check('processos é lista', isinstance(doc['processos'], list))
    check('texto vai inteiro para o índice',
          doc['texto'] == (artigo.texto or ''), '(texto divergente)')


def test_indexar_e_buscar():
    """Ponta a ponta contra o Meilisearch local, em índice de teste próprio."""
    print('\n7. Indexação e busca (Meilisearch local)')

    if not busca.is_available():
        print('  ⏭️  Meilisearch não responde — pulando')
        return

    from main import app
    from app.models import DouArticle

    # NUNCA o índice de produção: neste módulo já houve teste que destruiu
    # dado real por usar a mesma chave que o dado verdadeiro.
    nome_teste = 'dou_articles_test'
    indice = busca.get_index(nome_teste)

    try:
        with app.app_context():
            artigos = DouArticle.query.limit(300).all()
            if not artigos:
                print('  ⏭️  acervo vazio — pulando')
                return
            enviados = busca.index_articles(artigos, indice=indice)

        check('indexou o lote', enviados == len(artigos), f'{enviados}/{len(artigos)}')

        with app.app_context():
            com_cnpj = next((a for a in artigos if busca.extrair_cnpjs(a.texto)), None)

        if com_cnpj is not None:
            cnpj = busca.extrair_cnpjs(com_cnpj.texto)[0]
            r = busca.search(cnpj, indice=indice)
            check('acha pelo CNPJ só em dígitos',
                  any(h['id'] == com_cnpj.id for h in r['hits']),
                  f'{cnpj}: {r["total"]} resultado(s)')

            formatado = f'{cnpj[:2]}.{cnpj[2:5]}.{cnpj[5:8]}/{cnpj[8:12]}-{cnpj[12:]}'
            r2 = busca.search(formatado, indice=indice)
            check('acha pelo CNPJ formatado, mesmo resultado',
                  any(h['id'] == com_cnpj.id for h in r2['hits']), formatado)

        r3 = busca.search('portaria', indice=indice)
        check('busca por texto devolve resultados', r3['total'] > 0, str(r3['total']))
        check('traz facetas com contagem',
              isinstance(r3['facetas'], dict) and 'pub_name' in r3['facetas'],
              str(list(r3['facetas'].keys()) if r3['facetas'] else None))
        check('traz trecho destacado',
              any('<mark>' in (h.get('trecho') or '') for h in r3['hits']),
              '(nenhum destaque)')

        r4 = busca.search('zzzznaoexisteestetermo', indice=indice)
        check('termo sem resultado devolve zero, sem erro', r4['total'] == 0, str(r4['total']))

    finally:
        busca.drop_index(nome_teste)
        print('  (índice de teste removido)')
```

E registrar as duas em `main()`:

```python
    test_orgao_raiz()
    test_documento_indexado()
    test_indexar_e_buscar()
```

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
uv run python tests/test_dou_search.py
```

Esperado: `AttributeError: module 'app.services.dou_search_service' has no attribute 'montar_documento'`.

- [ ] **Step 3: Implementar índice e indexação**

Acrescentar a `app/services/dou_search_service.py`:

```python
import logging
import os

from meilisearch_python_sdk import Client as MeilisearchClient
from meilisearch_python_sdk.models.settings import TypoTolerance

logger = logging.getLogger(__name__)

MEILISEARCH_HOST = os.getenv('MEILISEARCH_HOST', 'http://localhost:7700')
MEILISEARCH_API_KEY = os.getenv('MEILISEARCH_API_KEY')
MEILI_INDEX = os.getenv('DOU_MEILI_INDEX', 'dou_articles')

# A ordem é a ordem de relevância: o Meilisearch pesa os primeiros mais alto.
_SEARCHABLE = ['identifica', 'ementa', 'titulo', 'orgao_hierarquia',
               'cnpjs', 'processos', 'texto']
_FILTERABLE = ['pub_name', 'pub_date_num', 'art_type', 'orgao_raiz', 'edicao']
_SORTABLE = ['pub_date_num']

# Número aproximado é número errado: sem isso, 19630496000105 casaria com o
# CNPJ de outra empresa que difere por um dígito.
_SEM_TOLERANCIA = ['cnpjs', 'processos']

LOTE_PADRAO = 1000


def _client() -> MeilisearchClient:
    return MeilisearchClient(MEILISEARCH_HOST, MEILISEARCH_API_KEY)


def is_available() -> bool:
    """O Meilisearch responde? Falha aqui nunca vira exceção para quem chama."""
    try:
        _client().health()
        return True
    except Exception as exc:  # noqa: BLE001 — indisponibilidade não é erro de programa
        logger.info('DOU busca: Meilisearch indisponível (%s)', exc)
        return False


def get_index(nome: str | None = None):
    """Índice pronto para uso, com os atributos aplicados de forma idempotente."""
    client = _client()
    indice = client.get_or_create_index(uid=nome or MEILI_INDEX, primary_key='id')

    if list(indice.get_searchable_attributes() or []) != _SEARCHABLE:
        client.wait_for_task(
            indice.update_searchable_attributes(_SEARCHABLE).task_uid,
            timeout_in_ms=20000)

    if set(indice.get_filterable_attributes() or []) != set(_FILTERABLE):
        client.wait_for_task(
            indice.update_filterable_attributes(_FILTERABLE).task_uid,
            timeout_in_ms=20000)

    if set(indice.get_sortable_attributes() or []) != set(_SORTABLE):
        client.wait_for_task(
            indice.update_sortable_attributes(_SORTABLE).task_uid,
            timeout_in_ms=20000)

    client.wait_for_task(
        indice.update_typo_tolerance(
            TypoTolerance(enabled=True, disable_on_attributes=_SEM_TOLERANCIA)
        ).task_uid, timeout_in_ms=20000)

    return indice


def drop_index(nome: str) -> None:
    """Remove um índice. Existe para o teste limpar o índice dele."""
    try:
        _client().delete_index(nome)
    except Exception as exc:  # noqa: BLE001
        logger.warning('DOU busca: falha ao remover o índice %s: %s', nome, exc)


def montar_documento(artigo) -> dict:
    """DouArticle → documento do índice."""
    texto = artigo.texto or ''
    return {
        'id': artigo.id,
        'edition_id': artigo.edition_id,
        'identifica': artigo.identifica or '',
        'ementa': artigo.ementa or '',
        'titulo': artigo.titulo or '',
        'texto': texto,
        'orgao_hierarquia': artigo.orgao_hierarquia or '',
        'orgao_raiz': orgao_raiz(artigo.orgao_hierarquia) or '—',
        'art_type': artigo.art_type or '—',
        'pub_name': artigo.pub_name or '—',
        'edicao': artigo.edicao or '',
        'pagina': artigo.pagina or '',
        'pagina_num': artigo.pagina_num,
        'pdf_page': artigo.pdf_page or '',
        # AAAAMMDD inteiro, não timestamp: timestamp depende de fuso e erra o
        # dia no filtro por data.
        'pub_date_num': int(artigo.pub_date.strftime('%Y%m%d')) if artigo.pub_date else 0,
        'data_br': artigo.pub_date.strftime('%d/%m/%Y') if artigo.pub_date else '',
        'cnpjs': extrair_cnpjs(texto),
        'processos': extrair_processos(texto),
    }


def index_articles(artigos, indice=None) -> int:
    """Indexa uma coleção de DouArticle. Devolve quantos foram enviados.

    Falha aqui **nunca** derruba a captura: a fase 1 não pode passar a depender
    da fase 2. Erro é registrado e a matéria entra no índice na próxima
    reindexação.
    """
    artigos = list(artigos or [])
    if not artigos:
        return 0

    try:
        indice = indice or get_index()
        documentos = [montar_documento(a) for a in artigos]
        for inicio in range(0, len(documentos), LOTE_PADRAO):
            indice.add_documents(documentos[inicio:inicio + LOTE_PADRAO])
        return len(documentos)
    except Exception as exc:  # noqa: BLE001 — indexar não derruba a captura
        logger.error('DOU busca: falha ao indexar %d matéria(s): %s', len(artigos), exc)
        return 0


def reindex_all(desde=None, lote: int = LOTE_PADRAO, indice=None) -> int:
    """Reconstrói o índice a partir do banco. Devolve o total indexado.

    Percorre por id crescente em blocos, para não carregar centenas de milhares
    de matérias na memória de uma vez.
    """
    from app.models import DouArticle  # import tardio: evita ciclo com models

    indice = indice or get_index()
    query_base = DouArticle.query
    if desde is not None:
        query_base = query_base.filter(DouArticle.pub_date >= desde)

    total = 0
    ultimo_id = 0
    while True:
        bloco = (query_base.filter(DouArticle.id > ultimo_id)
                 .order_by(DouArticle.id).limit(lote).all())
        if not bloco:
            break
        indice.add_documents([montar_documento(a) for a in bloco])
        total += len(bloco)
        ultimo_id = bloco[-1].id
        logger.info('DOU busca: %d matéria(s) indexada(s)', total)

    return total
```

- [ ] **Step 4: Implementar `search()` mínimo para o teste rodar**

O teste da Task 2 usa `search()`. Acrescentar ao mesmo arquivo (a versão
completa, com filtros, vem na Task 3):

```python
_FACETAS = ['pub_name', 'orgao_raiz', 'art_type']
_DESTAQUE_PRE = '<mark>'
_DESTAQUE_POS = '</mark>'
TAM_TRECHO = 40


def search(termo: str, filtros: dict | None = None, ordem: str = 'relevancia',
           pagina: int = 1, por_pagina: int = 20, indice=None) -> dict:
    """Busca no acervo. Devolve sempre um dicionário, mesmo em falha.

    Chaves: hits, total, ms, facetas, tipo_consulta, indisponivel.
    """
    # `filtros` é aceito aqui e passa a ser aplicado na Task 3 — a assinatura
    # já nasce completa para a tela não precisar mudar depois.
    vazio = {'hits': [], 'total': 0, 'ms': 0, 'facetas': {},
             'tipo_consulta': 'texto', 'indisponivel': False}

    tipo, normalizado = classificar_consulta(termo)
    if not normalizado:
        return vazio

    try:
        indice = indice or get_index()
        resultado = indice.search(
            normalizado,
            offset=(max(pagina, 1) - 1) * por_pagina,
            limit=por_pagina,
            facets=_FACETAS,
            attributes_to_highlight=['identifica', 'ementa', 'texto'],
            highlight_pre_tag=_DESTAQUE_PRE,
            highlight_post_tag=_DESTAQUE_POS,
            attributes_to_crop=['texto'],
            crop_length=TAM_TRECHO,
            sort=['pub_date_num:desc'] if ordem == 'data' else None,
        )
    except Exception as exc:  # noqa: BLE001 — buscar não derruba a tela
        logger.error('DOU busca: falha na consulta %r: %s', termo, exc)
        return {**vazio, 'indisponivel': True}

    return {
        'hits': [_formatar_hit(h) for h in resultado.hits],
        'total': resultado.estimated_total_hits or 0,
        'ms': resultado.processing_time_ms or 0,
        'facetas': resultado.facet_distribution or {},
        'tipo_consulta': tipo,
        'indisponivel': False,
    }


def _formatar_hit(hit: dict) -> dict:
    """Achata o resultado do Meilisearch no que a tela precisa."""
    destacado = hit.get('_formatted') or {}
    return {
        'id': hit.get('id'),
        'identifica': destacado.get('identifica') or hit.get('identifica') or '(sem identificação)',
        'ementa': destacado.get('ementa') or hit.get('ementa') or '',
        'trecho': destacado.get('texto') or '',
        'orgao_hierarquia': hit.get('orgao_hierarquia') or '',
        'orgao_raiz': hit.get('orgao_raiz') or '',
        'art_type': hit.get('art_type') or '',
        'pub_name': hit.get('pub_name') or '',
        'data_br': hit.get('data_br') or '',
        'pagina': hit.get('pagina') or '',
    }
```

- [ ] **Step 5: Rodar o teste e confirmar que passa**

```bash
uv run python tests/test_dou_search.py
```

Esperado: `✅ Todos os testes passaram`, incluindo achar a matéria pelo CNPJ em
dígitos e pelo formatado. Se "acha pelo CNPJ só em dígitos" falhar, o campo
`cnpjs` não foi para os atributos pesquisáveis — confira `_SEARCHABLE`.

- [ ] **Step 6: Ligar a indexação à captura**

Em `app/services/dou_ingestion_service.py`, dentro de `ingest_date`, logo após
o `db.session.commit()` do bloco `try` de cada seção:

```python
            db.session.commit()

            # Índice de busca: alimentado depois do commit, e de propósito sem
            # try/except aqui — index_articles já trata a falha internamente e
            # devolve 0. A captura não pode passar a depender do Meilisearch.
            from app.services import dou_search_service
            dou_search_service.index_articles(
                DouArticle.query.filter_by(edition_id=edition.id).all()
            )
```

- [ ] **Step 7: Escrever o CLI de reindexação**

Criar `scripts/reindex_dou.py`:

```python
#!/usr/bin/env python3
"""
Reindexa o acervo do DOU no Meilisearch.

O índice é descartável: o MySQL é a fonte da verdade. Use este script na carga
inicial, depois de um backfill, ou se o índice for perdido.

    uv run python scripts/reindex_dou.py
    uv run python scripts/reindex_dou.py --desde 2026-08-01
    uv run python scripts/reindex_dou.py --recriar     # apaga o índice antes
"""

from __future__ import annotations

import argparse
import sys
from datetime import datetime
from pathlib import Path

project_root = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(project_root))

from dotenv import load_dotenv  # type: ignore[import]
load_dotenv(project_root / '.env')


def _log(msg: str) -> None:
    print(f"[{datetime.now().strftime('%Y-%m-%d %H:%M:%S')}] {msg}", flush=True)


def _parse_data(valor: str):
    try:
        return datetime.strptime(valor, '%Y-%m-%d').date()
    except ValueError:
        raise argparse.ArgumentTypeError(f'data inválida: {valor} (use YYYY-MM-DD)')


def main() -> int:
    parser = argparse.ArgumentParser(description='Reindexa o acervo do DOU')
    parser.add_argument('--desde', type=_parse_data, help='só a partir desta data')
    parser.add_argument('--recriar', action='store_true',
                        help='apaga o índice antes de reconstruir')
    args = parser.parse_args()

    from main import app
    from app.services import dou_search_service as busca

    if not busca.is_available():
        _log('⚠️  Meilisearch não responde — nada a fazer')
        return 1

    if args.recriar:
        _log(f'🗑️  removendo o índice {busca.MEILI_INDEX}...')
        busca.drop_index(busca.MEILI_INDEX)

    with app.app_context():
        _log('⏳ reindexando...')
        total = busca.reindex_all(desde=args.desde)
        _log(f'✅ {total} matéria(s) indexada(s)')

    return 0


if __name__ == '__main__':
    sys.exit(main())
```

- [ ] **Step 8: Rodar a reindexação real e conferir**

```bash
uv run python scripts/reindex_dou.py --recriar
```

Esperado: indexa o total de matérias do banco. Confirmar que a contagem bate:

```bash
uv run python -c "
from main import app
from app.models import DouArticle
from app.services import dou_search_service as b
with app.app_context():
    no_banco = DouArticle.query.count()
b.get_index().update_settings  # força a criação
import time; time.sleep(2)
stats = b.get_index().get_stats()
print('no banco :', no_banco)
print('no índice:', stats.number_of_documents)
print('bate?', no_banco == stats.number_of_documents)
"
```

Se o índice tiver menos documentos, o Meilisearch ainda está processando a
fila — aguarde alguns segundos e repita.

- [ ] **Step 9: Commit**

```bash
git add app/services/dou_search_service.py app/services/dou_ingestion_service.py scripts/reindex_dou.py tests/test_dou_search.py
git commit -m "feat(dou): índice Meilisearch do acervo, indexação e reindexação

Um documento por matéria, com CNPJ e número de processo em campos próprios
e normalizados. Tolerância a erro desligada nesses dois campos: número
aproximado é número errado.

A captura passa a indexar as matérias após o commit de cada (dia, seção),
mas index_articles trata a própria falha e devolve 0 — a fase 1 não pode
passar a depender do Meilisearch.

pub_date_num é AAAAMMDD inteiro, não timestamp, porque timestamp depende de
fuso e erra o dia no filtro."
```

---

## Task 3: Consulta com filtros e facetas

**Files:**
- Modify: `app/services/dou_search_service.py`
- Test: `tests/test_dou_search.py`

**Interfaces:**
- Consumes: `search`, `get_index`, `montar_documento` da Task 2.
- Produces: `search(termo, filtros, ordem, pagina, por_pagina, indice)` aceitando
  `filtros = {'pub_name': [...], 'orgao_raiz': [...], 'art_type': [...], 'de': date, 'ate': date}`
  e `montar_filtro(filtros) -> str | None`.

- [ ] **Step 1: Escrever o teste que falha**

Acrescentar a `tests/test_dou_search.py`, antes de `def main():`:

```python
def test_montar_filtro():
    print('\n8. Montagem do filtro do Meilisearch')
    from datetime import date

    check('sem filtro devolve None', busca.montar_filtro({}) is None,
          repr(busca.montar_filtro({})))

    f = busca.montar_filtro({'pub_name': ['DO1']})
    check('um valor vira igualdade', f == '(pub_name = "DO1")', repr(f))

    f = busca.montar_filtro({'pub_name': ['DO1', 'DO3']})
    check('dois valores do mesmo campo viram OR',
          f == '(pub_name = "DO1" OR pub_name = "DO3")', repr(f))

    f = busca.montar_filtro({'pub_name': ['DO1'], 'art_type': ['Portaria']})
    check('campos diferentes viram AND',
          ' AND ' in f and 'pub_name' in f and 'art_type' in f, repr(f))

    f = busca.montar_filtro({'de': date(2026, 8, 1), 'ate': date(2026, 8, 10)})
    check('período vira faixa em AAAAMMDD',
          'pub_date_num >= 20260801' in f and 'pub_date_num <= 20260810' in f, repr(f))

    f = busca.montar_filtro({'orgao_raiz': ['Ministério "X"']})
    check('aspas no valor são escapadas', '\\"' in f, repr(f))


def test_busca_com_filtro():
    print('\n9. Busca filtrada (Meilisearch local)')
    if not busca.is_available():
        print('  ⏭️  Meilisearch não responde — pulando')
        return

    from main import app
    from app.models import DouArticle

    nome_teste = 'dou_articles_test'
    indice = busca.get_index(nome_teste)
    try:
        with app.app_context():
            artigos = DouArticle.query.filter_by(pub_name='DO1').limit(200).all()
            if not artigos:
                print('  ⏭️  sem matérias da Seção 1 — pulando')
                return
            busca.index_articles(artigos, indice=indice)

        sem = busca.search('portaria', indice=indice)
        com = busca.search('portaria', filtros={'pub_name': ['DO2']}, indice=indice)
        check('filtro por seção reduz o resultado', com['total'] <= sem['total'],
              f"{sem['total']} -> {com['total']}")
        check('filtro por seção inexistente no lote zera', com['total'] == 0,
              str(com['total']))

        por_data = busca.search('portaria', ordem='data', indice=indice)
        datas = [h['data_br'] for h in por_data['hits'] if h['data_br']]
        check('ordem por data é decrescente',
              datas == sorted(datas, key=lambda d: d.split('/')[::-1], reverse=True),
              str(datas[:4]))
    finally:
        busca.drop_index(nome_teste)
        print('  (índice de teste removido)')
```

E registrar em `main()`:

```python
    test_indexar_e_buscar()
    test_montar_filtro()
    test_busca_com_filtro()
```

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
uv run python tests/test_dou_search.py
```

Esperado: `AttributeError: ... has no attribute 'montar_filtro'`.

- [ ] **Step 3: Implementar `montar_filtro` e ligar ao `search`**

Acrescentar a `app/services/dou_search_service.py`:

```python
def _escapar(valor: str) -> str:
    """O filtro do Meilisearch delimita valores por aspas duplas."""
    return str(valor).replace('\\', '\\\\').replace('"', '\\"')


def montar_filtro(filtros: dict | None) -> str | None:
    """Dicionário de filtros → expressão de filtro do Meilisearch.

    Valores do mesmo campo combinam com OR (marcar duas seções mostra as
    duas); campos diferentes combinam com AND (seção E órgão).
    """
    if not filtros:
        return None

    partes = []

    for campo in _FACETAS:
        valores = [v for v in (filtros.get(campo) or []) if v]
        if valores:
            ors = ' OR '.join(f'{campo} = "{_escapar(v)}"' for v in valores)
            partes.append(f'({ors})')

    de, ate = filtros.get('de'), filtros.get('ate')
    if de:
        partes.append(f"pub_date_num >= {int(de.strftime('%Y%m%d'))}")
    if ate:
        partes.append(f"pub_date_num <= {int(ate.strftime('%Y%m%d'))}")

    return ' AND '.join(partes) if partes else None
```

E, em `search`, passar o filtro à consulta — trocar a chamada `indice.search(...)`
para incluir:

```python
            filter=montar_filtro(filtros),
```

logo após o argumento `limit=por_pagina,`.

- [ ] **Step 4: Rodar o teste e confirmar que passa**

```bash
uv run python tests/test_dou_search.py
```

Esperado: `✅ Todos os testes passaram`.

- [ ] **Step 5: Commit**

```bash
git add app/services/dou_search_service.py tests/test_dou_search.py
git commit -m "feat(dou): filtros e facetas na busca

Valores do mesmo campo combinam com OR e campos diferentes com AND — marcar
duas seções mostra as duas, mas seção e órgão se somam. Período vira faixa
sobre pub_date_num."
```

---

## Task 4: A tela `/dou/busca`

**Files:**
- Modify: `app/blueprints/dou.py`
- Create: `templates/dou/busca.html`
- Modify: `static/css/dou.css`
- Modify: `templates/partials/sidebar.html`
- Test: `tests/test_dou_routes.py`

**Interfaces:**
- Consumes: `dou_search_service.search`, `.is_available`, `.classificar_consulta`.
- Produces: endpoint `dou.busca`.

- [ ] **Step 1: Escrever o teste que falha**

Acrescentar a `tests/test_dou_routes.py`, antes de `def main():`:

```python
def test_tela_de_busca():
    print('\n6. Tela de busca')
    endpoints = {r.endpoint for r in app.url_map.iter_rules()}
    check('dou.busca existe', 'dou.busca' in endpoints)

    with app.app_context():
        usuario = User.query.filter_by(role='admin').first()
        if usuario is None:
            print('  ⏭️  nenhum usuário admin no banco — pulando')
            return
        user_id, firm_id = usuario.id, usuario.law_firm_id

    with app.test_client() as c:
        with c.session_transaction() as sessao:
            sessao['user_id'] = user_id
            sessao['law_firm_id'] = firm_id
            sessao['user_role'] = 'admin'

        resposta = c.get('/dou/busca')
        check('abre sem termo (estado inicial)', resposta.status_code == 200,
              str(resposta.status_code))
        html = resposta.get_data(as_text=True)
        check('estado inicial, sem resultados', 'dou-hit' not in html)
        check('convida a agir', 'O que você procura' in html)

        resposta = c.get('/dou/busca?q=portaria')
        check('busca por termo responde 200', resposta.status_code == 200,
              str(resposta.status_code))

        resposta = c.get('/dou/busca?q=' + 'z' * 30)
        check('termo sem resultado responde 200, não 500',
              resposta.status_code == 200, str(resposta.status_code))
```

E registrar em `main()`: `test_tela_de_busca()`.

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
uv run python tests/test_dou_routes.py
```

Esperado: falha em `dou.busca existe`.

- [ ] **Step 3: Implementar a rota**

Em `app/blueprints/dou.py`, acrescentar o import do serviço no topo:

```python
from app.services import dou_search_service as busca_service
```

e a rota, logo após `def edicao(...)`:

```python
PER_PAGE_BUSCA = 20


@dou_bp.route('/busca')
def busca():
    """Busca por termo em todo o acervo.

    O acervo é global, mas a lista de clientes do atalho é do escritório — é o
    único ponto desta tela que enxerga tenant.
    """
    termo = (request.args.get('q') or '').strip()
    ordem = 'data' if request.args.get('ordem') == 'data' else 'relevancia'
    pagina = request.args.get('page', 1, type=int)

    filtros = {
        'pub_name': request.args.getlist('secao'),
        'orgao_raiz': request.args.getlist('orgao'),
        'art_type': request.args.getlist('tipo'),
        'de': _parse_data(request.args.get('de')),
        'ate': _parse_data(request.args.get('ate')),
    }

    resultado = None
    if termo:
        resultado = busca_service.search(
            termo, filtros=filtros, ordem=ordem,
            pagina=pagina, por_pagina=PER_PAGE_BUSCA)

    law_firm_id = session.get('law_firm_id')
    clientes = (Client.query.filter_by(law_firm_id=law_firm_id)
                .order_by(Client.name).all()) if law_firm_id else []

    return render_template(
        'dou/busca.html',
        termo=termo, resultado=resultado, filtros=filtros, ordem=ordem,
        pagina=pagina, por_pagina=PER_PAGE_BUSCA, clientes=clientes,
        disponivel=busca_service.is_available(),
        section_labels=DouEdition.SECTION_LABELS,
    )
```

Acrescentar `session` e `Client` aos imports do arquivo:

```python
from flask import (Blueprint, render_template, request, redirect,
                   url_for, flash, send_file, abort, session)
...
from app.models import db, DouEdition, DouArticle, DouSyncRun, Client
```

- [ ] **Step 4: Criar a tela**

Criar `templates/dou/busca.html`:

```html
{% extends "layout/base.html" %}
{% from "partials/page_hero.html" import page_hero %}

{% block title %}Buscar no Diário Oficial{% endblock %}

{% block extra_css %}
<link rel="stylesheet" href="{{ url_for('static', filename='css/dou.css') }}">
{% endblock %}

{% block content %}
<div class="dou-page">

{{ page_hero(
     'Buscar no Diário Oficial',
     'Pesquisa em todas as matérias capturadas',
     'bi bi-search',
     [{'label': 'Diário Oficial', 'href': url_for('dou.edicoes')},
      {'label': 'Busca', 'active': True}]) }}

<form method="get" class="card border-0 shadow-sm mb-3">
  <div class="card-body">
    <div class="row g-2">
      <div class="col-lg-7">
        <input type="search" name="q" value="{{ termo }}" autofocus
               class="form-control" placeholder="Termo, CNPJ ou número de processo">
      </div>
      <div class="col-lg-3">
        <select class="form-select" id="seletor-cliente">
          <option value="">Buscar por cliente…</option>
          {% for cliente in clientes %}
          <option value="{{ cliente.cnpj }}">{{ cliente.name }}</option>
          {% endfor %}
        </select>
      </div>
      <div class="col-lg-2 d-grid">
        <button type="submit" class="btn btn-primary">
          <i class="bi bi-search me-1"></i>Buscar
        </button>
      </div>
    </div>
    {% for chave, valores in {'secao': filtros.pub_name, 'orgao': filtros.orgao_raiz, 'tipo': filtros.art_type}.items() %}
      {% for v in valores %}<input type="hidden" name="{{ chave }}" value="{{ v }}">{% endfor %}
    {% endfor %}
    <input type="hidden" name="ordem" value="{{ ordem }}">
  </div>
</form>

{% if not disponivel %}
<div class="alert alert-warning">
  <i class="bi bi-exclamation-triangle me-1"></i>
  A busca está indisponível no momento — o serviço de indexação não respondeu.
  Você ainda pode <a href="{{ url_for('dou.edicoes') }}">navegar pelo acervo</a>.
</div>
{% elif not termo %}
<div class="card border-0 shadow-sm">
  <div class="card-body text-center py-5">
    <i class="bi bi-search display-4 text-muted"></i>
    <h5 class="mt-3">O que você procura no Diário?</h5>
    <p class="text-muted mb-0">
      Um termo (<em>fator acidentário</em>), um CNPJ (com ou sem pontuação)
      ou um número de processo. Ou escolha um cliente na lista acima.
    </p>
  </div>
</div>
{% elif resultado.indisponivel %}
<div class="alert alert-danger">
  A busca falhou. Tente de novo em instantes.
</div>
{% else %}

<div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3">
  <div class="dou-resumo">
    <strong>{{ '{:,}'.format(resultado.total).replace(',', '.') }}</strong>
    {{ 'matéria encontrada' if resultado.total == 1 else 'matérias encontradas' }}
    · {{ resultado.ms }} ms
    {% if resultado.tipo_consulta == 'cnpj' %}
    <span class="badge text-bg-light border ms-1">busca por CNPJ</span>
    {% elif resultado.tipo_consulta == 'processo' %}
    <span class="badge text-bg-light border ms-1">busca por processo</span>
    {% endif %}
  </div>
  <div class="btn-group btn-group-sm">
    <a class="btn btn-outline-secondary {{ 'active' if ordem == 'relevancia' }}"
       href="{{ url_for('dou.busca', q=termo, ordem='relevancia') }}">Relevância</a>
    <a class="btn btn-outline-secondary {{ 'active' if ordem == 'data' }}"
       href="{{ url_for('dou.busca', q=termo, ordem='data') }}">Mais recentes</a>
  </div>
</div>

<div class="row g-3">
  <div class="col-lg-3">
    <div class="card border-0 shadow-sm">
      <div class="card-header bg-white fw-semibold">Refinar</div>
      <div class="card-body dou-facetas">
        <form method="get">
          <input type="hidden" name="q" value="{{ termo }}">
          <input type="hidden" name="ordem" value="{{ ordem }}">

          {% for campo, rotulo, param, selecionados in [
               ('pub_name', 'Seção', 'secao', filtros.pub_name),
               ('orgao_raiz', 'Órgão', 'orgao', filtros.orgao_raiz),
               ('art_type', 'Tipo de ato', 'tipo', filtros.art_type)] %}
            {% set dist = resultado.facetas.get(campo) or {} %}
            {% if dist %}
            <div class="dou-faceta">
              <div class="dou-faceta__titulo">{{ rotulo }}</div>
              {% for valor, qtd in dist | dictsort(by='value', reverse=true) %}
              {% if loop.index <= 8 %}
              <label class="dou-faceta__item">
                <input type="checkbox" name="{{ param }}" value="{{ valor }}"
                       {{ 'checked' if valor in selecionados }}>
                <span class="dou-faceta__rot" title="{{ valor }}">
                  {{ section_labels.get(valor, valor) if campo == 'pub_name' else valor }}
                </span>
                <span class="dou-faceta__qtd">{{ qtd }}</span>
              </label>
              {% endif %}
              {% endfor %}
            </div>
            {% endif %}
          {% endfor %}

          <div class="dou-faceta">
            <div class="dou-faceta__titulo">Período</div>
            <div class="d-flex gap-1">
              <input type="date" name="de" class="form-control form-control-sm"
                     value="{{ filtros.de.isoformat() if filtros.de else '' }}">
              <input type="date" name="ate" class="form-control form-control-sm"
                     value="{{ filtros.ate.isoformat() if filtros.ate else '' }}">
            </div>
          </div>

          <div class="d-grid gap-1 mt-3">
            <button type="submit" class="btn btn-primary btn-sm">Aplicar</button>
            <a href="{{ url_for('dou.busca', q=termo) }}"
               class="btn btn-outline-secondary btn-sm">Limpar filtros</a>
          </div>
        </form>
      </div>
    </div>
  </div>

  <div class="col-lg-9">
    {% if not resultado.hits %}
    <div class="card border-0 shadow-sm">
      <div class="card-body text-center py-5 text-muted">
        <i class="bi bi-search display-6"></i>
        <p class="mt-2 mb-0">
          Nada encontrado para <strong>{{ termo }}</strong>.
          Tente outro termo ou remova filtros.
        </p>
      </div>
    </div>
    {% else %}
    {% for h in resultado.hits %}
    <a class="dou-hit" href="{{ url_for('dou.materia', article_id=h.id) }}">
      <div class="dou-hit__titulo">{{ h.identifica | safe }}</div>
      <div class="dou-hit__meta">
        {{ h.data_br }} · {{ h.pub_name }}{% if h.pagina %} · p. {{ h.pagina }}{% endif %}
        {% if h.art_type %} · {{ h.art_type }}{% endif %}
      </div>
      {% if h.trecho %}
      <div class="dou-hit__trecho">…{{ h.trecho | safe }}…</div>
      {% elif h.ementa %}
      <div class="dou-hit__trecho">{{ h.ementa | safe }}</div>
      {% endif %}
      <div class="dou-hit__orgao">{{ h.orgao_hierarquia }}</div>
    </a>
    {% endfor %}

    {% set total_paginas = (resultado.total // por_pagina) + (1 if resultado.total % por_pagina else 0) %}
    {% if total_paginas > 1 %}
    <div class="d-flex justify-content-between align-items-center mt-3">
      <small class="text-muted">Página {{ pagina }} de {{ total_paginas }}</small>
      <div class="btn-group btn-group-sm">
        {% if pagina > 1 %}
        <a class="btn btn-outline-secondary"
           href="{{ url_for('dou.busca', q=termo, ordem=ordem, secao=filtros.pub_name, orgao=filtros.orgao_raiz, tipo=filtros.art_type, page=pagina-1) }}">Anterior</a>
        {% endif %}
        {% if pagina < total_paginas %}
        <a class="btn btn-outline-secondary"
           href="{{ url_for('dou.busca', q=termo, ordem=ordem, secao=filtros.pub_name, orgao=filtros.orgao_raiz, tipo=filtros.art_type, page=pagina+1) }}">Próxima</a>
        {% endif %}
      </div>
    </div>
    {% endif %}
    {% endif %}
  </div>
</div>
{% endif %}

</div>

<script>
  // Escolher um cliente preenche a busca com o CNPJ dele e submete.
  document.getElementById('seletor-cliente')?.addEventListener('change', function () {
    if (!this.value) return;
    const campo = document.querySelector('input[name="q"]');
    campo.value = this.value;
    campo.form.submit();
  });
</script>
{% endblock %}
```

- [ ] **Step 5: Acrescentar os estilos**

Acrescentar ao final de `static/css/dou.css`:

```css
/* ------------------------------------------------------------ busca */

.dou-facetas {
  max-height: 62vh;
  overflow-y: auto;
}

.dou-faceta + .dou-faceta {
  margin-top: 1rem;
  padding-top: 1rem;
  border-top: 1px solid var(--bs-border-color);
}

.dou-faceta__titulo {
  font-size: .68rem;
  text-transform: uppercase;
  letter-spacing: .08em;
  color: var(--bs-secondary-color);
  margin-bottom: .4rem;
}

.dou-faceta__item {
  display: grid;
  grid-template-columns: auto minmax(0, 1fr) auto;
  gap: .45rem;
  align-items: center;
  padding: .12rem 0;
  font-size: .82rem;
  cursor: pointer;
}

.dou-faceta__rot {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.dou-faceta__qtd {
  font-size: .72rem;
  font-variant-numeric: tabular-nums;
  color: var(--bs-secondary-color);
}

/* Um resultado */
.dou-hit {
  display: block;
  padding: .85rem 1.05rem;
  border: 1px solid var(--bs-border-color);
  border-radius: 14px;
  background: var(--bs-body-bg);
  text-decoration: none;
  color: inherit;
  transition: transform .18s ease, box-shadow .18s ease, border-color .18s ease;
}

.dou-hit + .dou-hit {
  margin-top: .55rem;
}

.dou-hit:hover {
  transform: translateY(-2px);
  box-shadow: 0 12px 24px rgba(0, 0, 0, .07);
  border-color: var(--bs-primary-border-subtle);
}

.dou-hit__titulo {
  font-weight: 600;
  color: var(--bs-body-color);
}

.dou-hit:hover .dou-hit__titulo {
  color: var(--bs-primary);
}

.dou-hit__meta {
  font-size: .74rem;
  color: var(--bs-secondary-color);
  margin: .12rem 0 .3rem;
}

.dou-hit__trecho {
  font-size: .84rem;
  color: var(--bs-body-color);
  line-height: 1.5;
}

.dou-hit__orgao {
  font-size: .74rem;
  color: var(--bs-secondary-color);
  margin-top: .3rem;
}

/* O termo casado, destacado pelo Meilisearch */
.dou-hit mark {
  background: var(--bs-warning-bg-subtle);
  color: inherit;
  padding: 0 .12em;
  border-radius: 3px;
}

@media (prefers-reduced-motion: reduce) {
  .dou-hit { transition: none; }
  .dou-hit:hover { transform: none; }
}
```

- [ ] **Step 6: Acrescentar o item de menu**

Em `templates/partials/sidebar.html`, dentro do `<ul class="nav nav-treeview">`
do bloco `can_view_module('dou')`, antes do item "Captura":

```html
            <li class="nav-item">
              <a href="{{ url_for('dou.busca') }}"
                class="nav-link {{ 'active' if request.endpoint == 'dou.busca' else '' }}"
                style="font-size: 0.9em;">
                <i class="nav-icon bi bi-search" style="margin-right: 8px; color: #6c757d;"></i>
                <p style="margin-left: 15px;">Busca</p>
              </a>
            </li>
```

- [ ] **Step 7: Rodar os testes de rota**

```bash
uv run python tests/test_dou_routes.py
```

Esperado: `✅ Todos os testes passaram`.

- [ ] **Step 8: Conferir a tela com dados reais**

```bash
uv run python -c "
import re, sys; sys.path.insert(0,'.')
from main import app
from app.models import User
with app.app_context():
    u = User.query.filter_by(role='admin').first(); uid, fid = u.id, u.law_firm_id
with app.test_client() as c:
    with c.session_transaction() as s:
        s['user_id'], s['law_firm_id'], s['user_role'] = uid, fid, 'admin'
    h = c.get('/dou/busca?q=previdência').get_data(as_text=True)
    print('resultados :', re.search(r'([\d.]+)\s*matérias? encontrada', h).group(1) if 'encontrada' in h else 'nenhum')
    print('destaque   :', '<mark>' in h)
    print('facetas    :', h.count('dou-faceta__item'), 'opções')
    print('seletor    :', 'seletor-cliente' in h)
"
```

Esperado: resultados > 0, destaque presente, facetas listadas.

- [ ] **Step 9: Commit**

```bash
git add app/blueprints/dou.py templates/dou/busca.html static/css/dou.css templates/partials/sidebar.html tests/test_dou_routes.py
git commit -m "feat(dou): tela de busca com facetas e trecho destacado

Busca em todo o acervo, com refinamento por seção, órgão, tipo de ato e
período — cada faceta com a contagem, porque com centenas de milhares de
matérias filtrar sem contagem é filtrar às cegas.

O seletor de cliente preenche a busca com o CNPJ do cadastro. É o único
ponto da tela que enxerga escritório: o acervo continua global."
```

---

## Task 5: Documentação

**Files:**
- Modify: `CLAUDE.md`
- Modify: `docs/MANUAL_DIARIO_OFICIAL.md`

- [ ] **Step 1: Registrar no CLAUDE.md**

Na tabela da Camada de Serviços, após `dou_ingestion_service`:

```markdown
| `dou_search_service`                   | Busca no acervo do DOU (Meilisearch): extração e normalização de CNPJ/processo, indexação e consulta com facetas — fonte única da tela de busca |
```

Na seção "Integração INLABS / Diário Oficial da União", ao final:

```markdown
- **Busca (`dou_search_service`)**: índice Meilisearch `dou_articles`, um
  documento por matéria. O MySQL é a fonte da verdade e o índice é
  descartável — `scripts/reindex_dou.py` reconstrói. Indexar nunca derruba a
  captura; buscar nunca derruba a tela.
- **CNPJ e número de processo não podem ser buscados como texto.** O
  tokenizador quebra `19.630.496/0001-05` na pontuação, e quem digita
  `19630496000105` não acharia nada — em 31% do acervo. Os identificadores são
  extraídos, normalizados para só dígitos e guardados nos campos `cnpjs` e
  `processos`, com tolerância a erro desligada neles. A consulta é roteada por
  `classificar_consulta()`: 14 dígitos → CNPJ, 17 → processo, resto → texto.
```

No bloco de variáveis de ambiente, junto das demais do DOU:

```bash
DOU_MEILI_INDEX=dou_articles            # índice da busca
```

- [ ] **Step 2: Acrescentar a seção ao manual do usuário**

Em `docs/MANUAL_DIARIO_OFICIAL.md`, após a seção "3. A matéria" e antes de
"### PDF assinado":

```markdown
## Busca

A tela **Busca** procura em todas as matérias já capturadas, de todas as datas
e seções.

### O que dá para digitar

| Você digita | O sistema entende |
|---|---|
| `fator acidentário` | Termo livre — acha variações e ignora acentuação |
| `"fator acidentário de prevenção"` | Entre aspas, a expressão exata |
| `19.630.496/0001-05` ou `19630496000105` | CNPJ, com ou sem pontuação — dá no mesmo |
| `15414.630210/2026-80` | Número de processo administrativo |

> [!INFO] Para CNPJ e número de processo, a busca **não** aceita aproximação:
> um dígito diferente é outra empresa, outro processo. Para termos de texto,
> pequenos erros de digitação são tolerados.

### Buscar por cliente

A lista **"Buscar por cliente"**, ao lado do campo, traz os clientes
cadastrados no escritório. Escolher um preenche a busca com o CNPJ dele — é o
caminho mais rápido para responder "meu cliente apareceu no Diário?".

### Refinar o resultado

À esquerda, cada filtro mostra **quantas matérias** restam se você marcá-lo.
Assim você vê que dos 3.240 resultados, 412 são do Ministério da Previdência,
e chega lá num clique.

Pode ordenar por **relevância** (padrão) ou por **mais recentes**.

> [!ALERTA] A busca só encontra o que já foi capturado. Se um período não foi
> baixado, ele não aparece — confira a cobertura na aba Captura.
```

- [ ] **Step 3: Conferir que o manual renderiza**

```bash
uv run python -c "
from main import app
from app.services import manual_renderer
with app.app_context():
    mods = manual_renderer.render_modules()
    dou = [m for m in mods if m.get('id') == 'diario-oficial'][0]
    print('seção Busca presente:', 'Busca' in str(dou))
    print('callouts renderizados:', 'callout' in str(dou))
"
```

- [ ] **Step 4: Rodar toda a bateria**

```bash
uv run python tests/test_dou_search.py && \
uv run python tests/test_dou_routes.py && \
uv run python tests/test_dou_xml_parser.py && \
uv run python tests/test_dou_ingestion.py && \
uv run python tests/test_inlabs_client.py
```

Esperado: os cinco terminam com `✅ Todos os testes passaram`.

- [ ] **Step 5: Commit**

```bash
git add CLAUDE.md docs/MANUAL_DIARIO_OFICIAL.md
git commit -m "docs(dou): manual e CLAUDE.md da busca

Documenta o que mais confundiria quem chegar depois: CNPJ e número de
processo não podem ser buscados como texto, porque o tokenizador do
Meilisearch quebra na pontuação — daí os campos normalizados e o roteamento
da consulta."
```

---

## Após a implementação

1. **Rodar a carga inicial do índice:** `uv run python scripts/reindex_dou.py --recriar`
2. **Depois de cada backfill**, reindexar o período: `uv run python scripts/reindex_dou.py --desde 2026-04-13`
3. **Em produção**, garantir que o Meilisearch está no ar e que `MEILISEARCH_HOST` e `MEILISEARCH_API_KEY` estão no `.env`.

---

## Cobertura do spec

| Seção do spec | Task |
|---|---|
| §3 Arquitetura e as duas regras de degradação | Tasks 1–3 |
| §4.1 Extração de identificadores | Task 1 |
| §4.2 Roteamento da consulta | Task 1 |
| §4.3 Atalho por cliente | Task 4 |
| §5 Documento indexado | Task 2 |
| §6 A tela | Task 4 |
| §7 Sincronia do índice | Task 2 |
| §8 Tratamento de erro | Tasks 2–4 |
| §9 Testes | Tasks 1–4 |
| §10 Variáveis de ambiente | Tasks 2, 5 |
